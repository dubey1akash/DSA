/******************************************************************************
how to set ith bit from the left.
given n and i u should able to set bit i as a set 
that is the question
*******************************************************************************/
void setibit(int n,int i){
    int mask=(1<<i);
    n=(mask|n);
    
}
